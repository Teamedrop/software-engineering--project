#include <stdio.h>
#include "cs50.h"
#include <stdlib.h>
#include <time.h>

int main()
{
    srand(time(NULL));
    int test_sizes[] = {10, 5000, 20000, 50000};

    int num_sizes = sizeof(test_sizes) / sizeof(test_sizes[0]);

    for (int i = 0; i < size - 1; i++)
}